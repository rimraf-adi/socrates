# Start Socrates

# Terminal 1 - Backend (from /backend directory):
cd backend && uv run uvicorn app.main:app --reload

# Terminal 2 - Docker services (from root directory):
docker-compose up

# Terminal 3 - Frontend (from /frontend directory):
cd frontend && npm run dev
